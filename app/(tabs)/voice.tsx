import { View, Text } from 'react-native';

/**
 * Voice screen — placeholder (premium feature).
 * Full implementation comes in a later prompt.
 */
export default function VoiceScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-background">
      <Text className="text-textPrimary text-2xl font-semibold">Voice</Text>
      <Text className="text-textSecondary text-base mt-2">Voice conversation — coming soon</Text>
    </View>
  );
}
