// openai.ts is intentionally empty.
// All AI functions are now in:
//   gemini.ts  → aiChat(messages, systemPrompt, options)
//   stt.ts     → aiTranscribe(audioBase64, mimeType)
//   tts.ts     → aiTTS(text, options)
// To switch providers in future, edit only those files.
// Function signatures must stay the same.
